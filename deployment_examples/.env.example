# Docker Compose Environment Variables
# Copy this file to .env and adjust the values

# Path to your media directory on the host
# This should be the parent directory containing all media subdirectories
MEDIA_PATH=/path/to/your/media

# Hostname for the containers (optional, defaults to system hostname)
# HOSTNAME=my-docker-host
